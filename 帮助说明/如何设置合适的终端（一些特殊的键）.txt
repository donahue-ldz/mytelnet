设置终端
运行Telnet客户端程序的时候，需要设置一个适合Telnet程序运行的终端环境，在该环境中禁能了中断键^c、退出键^\、挂起键^z，当用户从键盘输入这些键的时候，不会造成Telnet客户端程序直接退出，而是会把这些数据也发送到远程主机上。设置终端由main.c中的ttysetup函数实现。其算法如下：
1. 备份原始的终端设置到全局变量oldtty中；
2. 取得原始终端设置的中断键、退出键、挂起键的值，保存到全局变量中函数会用到）；
3. 调用tcsetattr函数设置适合Telnet程序运行的终端环境，所做的改变是禁能了中断键、退出键、挂起键。
